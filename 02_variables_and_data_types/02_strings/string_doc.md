# f_strings - еужна, что б вставлять переменные в вывод:
# print(f'{username}: {message}')

# Для изминения регистра строки используються команды:
# username.lower(); username.upper(); username.title()

# Удаление лишних пробелов в начале и в конце строки:
# .lstrip(); .rstrip(); .strip()

# Символы табуляции и переноса на новую строку
# \t; \n;
